# 00 · 探针：先搞清楚用户有什么

> 读这个仓库的第一站。产出一个**配置向量**，后续所有选型都基于它。
> 字段定义见 [`services/_schema.md`](services/_schema.md#配置向量)。

探针分两类，**不要混**：

- **机器能查的** —— 直接跑命令，别问用户
- **必须问用户的** —— 探测不到，或答案取决于用户意愿而非硬件

问用户能查到的东西是浪费对方时间；替用户猜必须问的东西会出事。

---

## 一、机器能查的

### 1. 架构与平台

```sh
uname -srm
```

| 输出含 | 平台 | 往下走 |
|---|---|---|
| `Darwin ... arm64` | Apple Silicon Mac | §2a |
| `Darwin ... x86_64` | Intel Mac | §2a |
| `Linux ... x86_64` | 通用 x86 机器 / 软路由盒子 | §2b |
| `Linux ... aarch64` | ARM 单板 / 部分 NAS | §2b |
| **跑不了 `uname`**（提示不是内部命令） | **这是 Windows** | 见下 |

**如果这台机器是 Windows** —— 闲置的旧机器很可能是，[层 1](layers/1-substrate.md) 自己也这么说。
在 PowerShell 里查：

```powershell
$env:PROCESSOR_ARCHITECTURE          # AMD64 / ARM64
systeminfo | findstr /C:"OS 名称" /C:"OS Name"
```

⚠️ **本手册的主路径要求类 Unix 系统。** Windows 有两条出路，
**而它们的差别要在动手前讲清楚，因为其中一条会擦盘**：

| 出路 | 代价 |
|---|---|
| **重装成 Linux** | ⚠️ **会擦掉盘上现有数据**。`needs_human`，必须问用户，而且要先确认数据已经转移走 |
| **用它的 Linux 子系统（WSL2）** | 不擦盘，但多一层：开机自启和宿主休眠的行为都和原生不同，**把它当"另一台机器"来对待**。只跑几个服务可行。**旁路由不要走这条路**——理由见下 |

📌 **别默认用户愿意重装。** 那台机器上可能有他还在用的东西——
先问清楚，再谈方案。

> **为什么 WSL2 不做旁路由——把话说准：**
> 它**默认**是 NAT 网络。但 Windows 11 22H2 起另有 `networkingMode=mirrored`，
> 会镜像 Windows 的网络接口、支持组播、局域网也能直接连进 WSL
> （入站还受 Hyper-V 防火墙规则约束）。
>
> **所以"WSL2 的网络恒为 NAT、局域网连不进去"是不准确的。**
> 本手册仍然不把它列为旁路由候选，理由要写诚实：
> **我们没有验证过它在转发、开机自启、宿主休眠、回滚这几件事上的语义。**
> 这是"未验证"，不是"已排除"——**别拿一个错的技术理由去支撑一个保守的结论。**

NAS 另外确认厂商系统（QNAP QTS/QuTS、群晖 DSM、unRAID、TrueNAS）——它们的虚拟化与容器
是厂商套件，不是通用方案。

**架构决定的事**：容器镜像要有对应 arch 的构建。绝大多数主流服务都发 multi-arch，
但**动手前逐个查一遍 manifest**，别假设。查法：

```sh
docker manifest inspect <image>:<tag> | grep -A2 architecture
```

### 2a. 虚拟化能力：macOS

```sh
sysctl kern.hv_support        # 1 = 支持硬件加速虚拟化
which qemu-system-aarch64 qemu-system-x86_64 2>/dev/null
```

`kern.hv_support: 1` 意味着可以用 hvf 加速，VM 性能接近原生。

### 2b. 虚拟化能力：Linux

```sh
grep -cE 'vmx|svm' /proc/cpuinfo    # >0 = CPU 支持
ls -l /dev/kvm 2>/dev/null          # 存在且可读写 = KVM 可用
```

`/dev/kvm` 不存在通常是 BIOS 里没开 VT-x/AMD-V，**这要用户去 BIOS 开，属于问人的事**。

### 2c. 虚拟化能力：NAS

厂商套件是否已安装并可用（QNAP 的 Virtualization Station / Container Station、
群晖的 Virtual Machine Manager / Container Manager）。查不到就当没有，别猜型号支持情况。

### 3. 内存与存储

```sh
# Linux
free -h; lsblk -o NAME,SIZE,FSTYPE,MOUNTPOINT,TRAN
# macOS
sysctl hw.memsize; diskutil list; mount | grep -E '^/dev'
```

要记下的是每块盘的：**容量、文件系统、是不是外接、走什么总线（USB / 内置 / 网络挂载）**。
文件系统与总线直接决定它能不能承载数据库——见 [`layers/5-storage.md`](layers/5-storage.md)。

### 4. 这台机器上已经跑着什么

**如果这不是一台空机器，先盘点，再动它。**

```sh
docker ps -a --format 'table {{.Names}}\t{{.Image}}\t{{.Ports}}\t{{.Status}}'
docker volume ls
ss -tulpn | grep LISTEN        # 谁占了哪个端口
```

要记下来的：**容器名、镜像与版本、端口、数据卷落点、重启策略**。

⚠️ **在改网络之前，先从客户端确认每个既有服务现在是正常的**，并记下来。
没有这个基线，桥接或改网关之后出问题，你**分不清是新方案坏了还是它本来就坏**——
也无法证明回滚之后恢复了原状。

⚠️ 这份盘点同时是端口表和落点表的起点，见
[`services/_deployment.md`](services/_deployment.md#要自己维护的两张表)。

### 5. 网卡

```sh
# Linux
ip -br link
# macOS
networksetup -listallhardwareports
```

**数一下有几个可用网口。** 这不是凑数：桥接操作会短暂中断被桥接的那张网卡，
**有第二条路（另一张网卡 / Wi-Fi）才能在出问题时不失联**。只有一个网口时，
高风险操作必须在物理接触得到机器的场合做。

### 6. 现有网络位置

```sh
# Linux
ip route | grep default; cat /etc/resolv.conf
# macOS
route -n get default; scutil --dns | grep nameserver | head
```

记下当前网关与 DNS。**如果发现默认网关指向一台已经不存在的设备，先解决这个**——
残留路由会造成"有的进程能连、有的连不上"这类极难诊断的间歇故障，且症状不指向真凶。

---

### 7. IPv6 现状

**要做代理的话这一步不能省**，否则"全屋代理"的结论会超出你实际测过的范围。

```sh
# 客户端有没有 IPv6 默认路由
ip -6 route show default            # Linux
netstat -rn -f inet6 | grep default # macOS

# 实际出网用的是哪一族
curl -s -4 <回显出口 IP 的服务>; curl -s -6 <回显出口 IP 的服务>
```

判定：

| 结果 | 含义 |
|---|---|
| 没有 IPv6 默认路由 | 简单——只需要管好 IPv4 |
| 有，且 `-6` 能出网 | ⚠️ **存在第二条出网路径**，处理方式见 [层 3](layers/3-proxy-stack.md#ipv6全屋代理绕不开的一问) |

⚠️ **这条查不清楚就不要宣布"全屋都走代理了"。** 双栈环境下最典型的故障是
"同一个站点时好时坏"——因为两次访问走了不同的协议族。

### 8. 全盘加密开了没有

**这条决定这台机器能不能在断电后自己回来。** 而且它**跟做不做旁路由无关**——
只要有任何东西被期望"没人操作也一直在"，就必须查这一条。

**下面三条是"把加密的卷列出来"，不是"这台机器加没加密"的是非题。**

```sh
# macOS
fdesetup status
# 形如: FileVault is On. / FileVault is Off.（忙碌时有单独退出码，-extended 给进度）

# Linux（LUKS）—— 列出所有 LUKS 块设备
lsblk -o NAME,FSTYPE,MOUNTPOINT | grep -i crypto_LUKS

# Windows —— 默认逐卷报告全机所有盘
manage-bde -status
# 只看系统盘：manage-bde -status C:
```

⚠️ **拿到输出之后必须再分两问，别从"有加密"直接跳到整机结论：**

| 问 | 为什么 |
|---|---|
| **① 启动 / 系统卷是怎么解锁的？** | 这决定机器**能不能开完机**。只有它挡着，才谈得上"断电后要有人来" |
| **② 服务的数据卷是怎么解锁的？** | 系统卷自动解锁**不等于**数据卷会自动挂上。数据卷没解开，服务照样起不来或看到空目录 |

**两问的组合才是结论：**

| 情况 | 含义 |
|---|---|
| 两个都不加密 | 这条不影响你，跳过 |
| **启动卷要人解锁** | ⚠️ **断电之后，机器在有人解锁之前不会回到可用状态。** 相册不备份、组网工具不上线、常驻 harness 不在——**而且不报错。** 有没有远程解锁的例外，见 [`ops/boot-persistence.md`](ops/boot-persistence.md#全盘加密会限制自恢复能力) |
| 启动卷自动过，**但数据卷要人解锁** | 机器能开机、能远程连上，**但依赖那块盘的服务起不来**——症状更隐蔽，因为机器看起来是好的 |

📌 **反过来也别误判**：一台根盘没加密、只有外接备份盘用了 LUKS 的机器，
`lsblk` 一样会命中——**那不代表它断电后回不来。**
Windows 上任一数据卷开了 BitLocker 也是同理。

⚠️ **开了加密要在部署前告诉用户。** "随时能从外面看照片"和"这台机器开了全盘加密"
是有冲突的——**把冲突摆到桌面上让用户选**，别默默假设有人会在家。

### 9. 已经装了什么组网 / 远程访问工具

**别把已经在跑的东西当成要新装的写。**

```sh
tailscale status 2>/dev/null | head -5

# 更通用：看有没有相关接口
ip -br addr | grep -iE 'tailscale|zt|wg'      # Linux
ifconfig | grep -iE 'utun|tailscale'           # macOS
```

装了的话**追问三件事**，否则层 6 会按"从零开始"写，跟现状打架：

- **这个组网账号是谁的？** 别人的账号意味着别人能改路由与访问策略
- **有没有开子网路由？** 有的话远端能到的不止这一台
- **它自己的域名解析开着吗？** 开着的话有一组名字要在代理侧做例外，见 [层 4](layers/4-routing-dns.md)

---

## 二、必须问用户的

以下探测不到，或答案取决于意愿。**逐条问清楚，不要替用户决定。**

### Q1. 主路由能不能给设备绑定固定 IP？

旁路由必须有稳定地址，否则客户端指过去的网关随时会失效。

- **能** → 让用户在主路由上绑定（**告诉他怎么做，不要代劳** —— 铁律①）
- **不能 / 不确定** → 旁路由内部配静态 IP，并提醒用户该地址可能与主路由的 DHCP 池冲突，
  需要把它排除在池外或选一个池外地址

### Q2a. 需不需要代理？

**先问这个，不要跳过。** 本手册的旁路由章节全部围绕透明代理展开，
但**"想要一台家庭服务器"和"想要代理"是两个独立的需求**，很多用户只要前者。

| 答案 | 结论 |
|---|---|
| **不需要** | `gateway: none`、`proxy: none`。**层 2、层 3、层 4 整片跳过**，直接去挑服务 |
| 需要 | 继续 Q2b |
| 不确定 / 没提过 | ⚠️ **当作"不需要"，并明确告诉用户你跳过了这部分。** 不要替用户假设 |

⚠️ **用户没主动提代理、订阅、机场、翻墙，就不要主动引入。** 装一套透明代理会
显著增加故障面和维护量，而且它是"全屋断网"这个风险的主要来源。
硬塞给一个只想备份照片的人，是帮倒忙。

### Q2b. 有装不了客户端代理的设备吗？

这个问题决定要不要做旁路由，比任何技术对比都重要。
**旁路由唯一不可替代的场景，就是"这台设备本身装不了代理"。**

| 你的设备 | `gateway` 取值方向 |
|---|---|
| **有**电视 / 游戏机 / 投影 / IoT 要走代理 | 需要旁路由 |
| 只有电脑和手机（都能自己装客户端） | `client-only`——更简单，也没有单点故障 |

→ 详见 [层 2](layers/2-gateway.md)。

### Q2c. 接管范围多大？

| 答案 | 说明 |
|---|---|
| 几台指定设备手动指网关 | **推荐的起步形态**，影响面可控 |
| **全屋，但排除某几台** | 常见的真实需求——想让电视和手机走，又不想动工作机 / NAS / 打印机。做法是全屋下发 + 例外设备改静态，**例外清单要在动手前列出来** |
| 全屋默认（主路由下发网关） | `risk: high`，必须分步走，必须问清楚 |

⚠️ **中间那一档最容易被漏掉。** 用户说"全屋"的时候往往心里有例外，
只是没说；而手册自己在三处要求排除设备（宿主、不该走代理的机器、IoT）。
**直接问"有没有哪几台不要走"，比事后补救便宜。**

📌 **先确认主路由做不做得到这件事**，再谈范围：

| 主路由能力 | 后果 |
|---|---|
| 能改 DHCP 下发的网关 / DNS | 全屋那两档才成立 |
| **改不了**（有些设备没有这个字段） | ⚠️ **本手册的全屋下发路径不成立**，只能缩小到逐台手动指网关。**这是要先问的，不是做到一半才发现的**。⚠️ 用户若提出换掉或接管主路由，**停下来说明那不在本手册范围**（铁律①） |
| 不确定 | 让用户去路由器管理页找 DHCP 设置，看有没有"默认网关 / 网关地址 / DNS"这几项可填 |

⚠️ **答"全屋"时，必须追问三件事** —— 否则"全屋"是个没有边界的词：

| 追问 | 为什么 |
|---|---|
| **承载旁路由的那台机器自己算不算？** | **不能算。** 宿主的网关指向跑在它自己身上的 VM 会造成开机死锁——见 [层 1](layers/1-substrate.md#判断二宿主自己不要走-vm)。这是硬约束，不是偏好 |
| **那台机器上已有的服务/容器算不算？** | 它们跟着宿主直连。**如果用户在意其中某个的出口（比如下载器），要单独处理**，不能默认它被"全屋"覆盖了 |
| **IPv6 算不算？** | 见 §7。只管了 IPv4 就说"全屋"，是超出实测范围 |

⚠️ **如果做不到用户理解的那个"全屋"，报告冲突，不要默默缩小它的含义。**

**不要默认用户想要全屋接管。** 全屋接管意味着旁路由变成单点故障——
它一挂，全家断网，包括根本不在乎代理的人。

⚠️ 用户说"我自己的电脑和电视能用就行"时，**这句话通常是在说"别影响家里其他人"**，
即接管范围的问题，不是"要不要旁路由"的问题。两件事分开确认。

工作室 / 微型企业尤其要问清楚：**断网即停工**。那里更该用"指定设备走"。

### Q3. 要不要从外网访问？

- **不要** → [`layers/6-ingress.md`](layers/6-ingress.md) **整层跳过**。省掉域名、证书、
  隧道账号一堆事，攻击面也小。
- **只有自己/同事要访问** → 优先 Tailscale，零公网暴露
- **要给外人访问（分享媒体、公开站点）** → 才需要隧道或端口转发

### Q4. 想跑哪些服务？

**先问既有的怎么办**（对照 §4 的盘点）：

| 对每个已有服务 | 含义 |
|---|---|
| **保留** | 不动它，但改网络时要保证它不受影响，且回滚后能恢复 |
| **替换** | 用手册里的模块重装——**先确认数据能迁移** |
| **不确定** | 当作"保留"，并告诉用户你没动它 |

⚠️ **不要因为用户在菜单里选了同类服务，就假设可以覆盖掉已有的那个。**
重复部署会撞端口、抢卷。

然后再给用户看 [`services/`](services/) 的清单挑新增的，不要预设。挑完再回头核对每个服务的
`requires`——尤其**存储要求**，这会反过来影响 Q5。

### Q5. 现有存储怎么用？

用 §3 查到的盘况，问用户每块盘的**用途意愿**。四种形态的能力边界见
[`layers/5-storage.md`](layers/5-storage.md)——那里不推荐任何一种，但每种都标了
"绝不能承载什么"。**先确认数据库类服务有合格的落点，再谈别的。**

### Q5b. 家里有哪些"难伺候"的客户端？

决定 [层 7 · 内网寻址](layers/7-lan-addressing.md) 怎么选。**问具体设备，别问抽象需求。**

| 有没有 | 影响 |
|---|---|
| 电视 / 机顶盒 | ❌ 基本不支持 `.local` 自动发现 → 需要 `lan-dns` |
| Android 手机 | ⚠️ mDNS 支持不完整，很多 App 解析不了 |
| 只有 Mac 和 iPhone | ✅ `.local` 开箱可用，可以先不折腾 DNS |

⚠️ **顺便问一句：有没有自己的域名？** 有的话，内网用它的子域是最省事的选择——
既不会和公网冲突，又能签真证书。这个问题在配 HTTPS 时才暴露，那时返工很烦。

### Q5c. 几个人会用这台机器？

**手册前面所有问题都是设备视角，这一条问的是人。**

| 答案 | 影响 |
|---|---|
| 只有我 | 跳过 [`ops/handover.md`](ops/handover.md) |
| 家里人也用 | 停机会影响别人，维护要挑时间 |
| **工作室 / 同事共用** | ⚠️ **断网即停工**；而且迟早要换人，交接从第一天就要考虑 |

⚠️ 多人共用时追问两件事：

- **谁能改配置？**（用服务和改配置是两种权限，别混）
- **共享的数据谁能删？** —— 共享素材库最常见的事故不是硬件坏，是有人删了别人还在用的东西

→ [`ops/handover.md`](ops/handover.md)

### Q6. 这台机器会关机或睡眠吗？

决定要不要做开机自恢复（[`ops/boot-persistence.md`](ops/boot-persistence.md)）。

⚠️ **就算不做旁路由，睡眠也有后果**，而且容易被忽略：

- 手机的照片自动备份 → 机器睡着时传不上去，得等它醒
- 电视上点开影音库 → 要先等机器唤醒，或者干脆看不到服务
- 定时任务（备份、清理）→ **可能整段丢掉**。补不补跑取决于**机器是睡着还是关机**
  加上**用的哪类调度**，`cron` 一律跳过，另外几种各有各的规矩，
  而且"补跑"常常是**多次错过合并成一次**。**别假设它会自己追上来** →
  [`ops/boot-persistence.md`](ops/boot-persistence.md#错过的定时任务会不会补跑)

⚠️ 如果这台机器**同时**要当旁路由又会睡眠，那更是硬矛盾：它睡了，
指着它的客户端直接断网。

笔记本尤其要问：**合盖会不会睡？** 很多人默认合盖=收起来，
而那正好是它该干活的时候。

---

## 三、产出配置向量

把上面的结论填成：

```yaml
# 单值
arch:       x86_64 | aarch64
gateway:    none | vm-openwrt | container-macvlan | dedicated-box | client-only
proxy:      none | nikki | shellcrash | openclash | homeproxy | client-side
ingress:    none | tailscale | tunnel | port-forward

# 多值（写成列表）
substrate:  [bare-metal, vm, container]
storage:    [internal, usb-portable, das-enclosure, nas]
addressing: [ip-static, hosts-file, lan-dns, mdns]
services:   [...]

# 现状事实（不是选型结果，但会限制选型——探到什么写什么）
disk_encryption: yes | no | unknown      # §8。yes = 不能承诺无人值守恢复
existing_mesh:   none | <已装的组网工具>  # §9。已装的话层 6 不按新装写
```

## 四、先对锚点

**拿向量去 [`blueprints/`](blueprints/) 对三条已验证路径**：

| 锚点 | 大致向量 |
|---|---|
| [A · 老笔记本一肩挑](blueprints/a-single-laptop.md) | `x86_64` + `vm` + `vm-openwrt`，服务栈也塞在同一个 VM 里 |
| [B · 混合](blueprints/b-hybrid-mini.md) | 服务栈原生跑，旁路由单独一个轻量 VM |
| [C · NAS 底座](blueprints/c-nas.md) | 厂商虚拟化套件 + 单臂旁路由（**未落地验证**） |

- **对得上** → 走那条，只读它引用的层，省掉逐层求解
- **对不上** → 进 [`layers/`](layers/)，从 [1-substrate](layers/1-substrate.md) 顺着往下

对不上是常态，不是异常。锚点是加速器，不是选项清单。
