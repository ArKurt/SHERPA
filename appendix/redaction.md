# 占位符约定

本仓库是公开手册，示例里不出现任何真实地址、域名、MAC 或凭证。
统一用下列占位符，**照抄不会连上任何东西**——每一处都要替换成你现场的真实值。

## 网络

| 占位符 | 含义 | 备注 |
|---|---|---|
| `192.0.2.1` | 主路由 | RFC 5737 TEST-NET-1，保留段，永远不会与真实网络冲突 |
| `192.0.2.4` | 旁路由 | |
| `192.0.2.10` | 服务器主机 | |
| `192.0.2.0/24` | 你的局域网段 | 真实环境常见 `192.168.x.0/24` |
| `198.51.100.7` | 客户端设备 | RFC 5737 TEST-NET-2，用于"另一台机器"的场景 |
| `203.0.113.9` | 公网 IP | RFC 5737 TEST-NET-3 |
| `100.64.0.0/10` | Tailscale CGNAT 段 | **这个是真的**，不要替换 |
| `AA:BB:CC:DD:EE:FF` | MAC 地址 | |

## 域名与凭证

| 占位符 | 含义 |
|---|---|
| `example.com` | 你的域名（RFC 2606 保留） |
| `service.example.com` | 某个服务的子域 |
| `<你的订阅链接>` | 代理订阅 URL——**永远不要写进任何仓库** |
| `<API_SECRET>` | 各类密钥 |
| `user@host` | SSH 目标 |

## 为什么用保留段而不是 `192.168.x.x`

`192.168.1.1` 之类看着自然，但正是**真实设备的常见地址**。agent 照抄示例、恰好命中用户
现场的真机，就会对着错误的目标执行——而且症状看起来像"配置生效了但行为不对"，极难往
"抄了示例值"上联想。

RFC 5737 的三段是**协议保证不会被路由**的文档专用地址。抄错立刻连不上，失败得干脆、
容易发现。**失败要响，不要闷。**

## 检查

```sh
scripts/check.sh redaction
```

拦下：私有网段地址、CGNAT 主机地址、真实 MAC、订阅链接痕迹、
未登记的外链域名、裸写的可疑域名、**内部主机名**。

## 凭证不只是"别写进文件"

**命令行参数同样是公开的。** `curl -H "Authorization: Bearer $TOKEN"` 里的变量
会被 shell 展开后才执行，值原样进 argv，同机谁都读得到。

正确写法与自查方法见 [`../ops/secrets.md`](../ops/secrets.md#情形二传给一条你手敲或-agent-敲的命令)。

## 分支约定

- **`develop`** —— 所有工作在这里。任何机器 `git pull` 就能接力。
- **`main`** —— 稳定状态。**develop 跑绿了就快进过去，没有导出脚本、没有要手工同步的差异。**

> 📌 **这套约定原来复杂得多**：main 曾计划做成"从 develop 导出、只写不读"，
> 因为 develop 上有 `reviews/` 这类不该进产品的东西。
> **审查材料整个移出仓库之后，那个差异消失了，规矩也就不必要了。**
>
> 少一条要维护的规矩，就少一处会漂的地方——这本手册已经因为
> "两处要人工同步"砍掉过双语 README。

📌 **仓库转公开时所有分支都是公开的。** 所以"哪个分支干净"不构成隐藏手段——
**脱敏要求对所有分支同样适用。**

📌 **审查过程材料不进这个仓库。** 五轮交叉审查的 13 份报告与全部裁决记录
存在作者本地的独立仓库里，**公开的这份只放中性的、普适的手册**。
留下的是证据链本身：`appendix/sources.md` 的 111 条断言出处，
以及提交消息里"为什么这么改"的推理。

## 发布前清理清单

仓库转公开之前，逐项确认：

- [ ] `scripts/check.sh` 全绿
- [x] ~~**删除 `REVIEW.md` 与 `reviews/`**~~ ✅ **2026-08-29 已执行**——
      连同历史一起摘除，另存为作者本地的独立仓库。指向它们的引用已全部改写。
      ⚠️ **当时才发现**：`check.sh` 的内网主机名检查原本**跳过 `reviews/`**，
      所以那个目录从来没被脱敏覆盖过，里面有 187 处私有知识库路径与机器名。
      摘除同时解决了这个问题，**并且那条排除现在也去掉了——检查从此覆盖全库。**
- [x] ~~**处理 git 历史里已知的两类泄漏**~~ ✅ **2026-08-29 已执行**（见本节末）（2026-08-28 全历史扫描结果，见下）

      **`check.sh` 查不到这些**——它只查工作树里的文件内容，不查历史、不查提交元数据。

      | 类型 | 具体 | 范围 |
      |---|---|---|
      | 内容 | 真实内网网关地址、内网主机名 | `reviews/round3-R2-codex.md`，6 个提交（工作树已脱敏，历史未动） |
      | 作者身份 | 一台 Mac 的内网主机名（`<主机名>@<主机名>.local` 形式） | `8de8ffa` |
      | 作者身份 | **另一个人的名字 + 一台机器的资产编号名** | `f06f6ca` |
      | 公开邮箱 | `6391657+ArKurt@users.noreply.github.com` | 56 个提交（是否算问题由作者定） |

      三条路，**推荐第二条**：

      1. 压成新仓库的初始提交 —— 干净，但**丢掉全部提交消息**。
         这个仓库的提交消息里存着"为什么这么改"，而"它是怎么被验证的"正是对外卖点之一。
      2. **`git filter-repo`**：替换字符串 + 用 `--mailmap` 把杂散作者归一。
         保住历史，代价是 force-push，其它机器上的 clone 要重新拉。
      3. 照原样公开 —— 泄漏留在历史里。

      ⚠️ **两次身份泄漏都来自"其他 agent / 其他机器直接提交"。**
      这就是分工表里"不要自己 commit、由主会话统一提交"那条规矩要防的东西。
      **下一轮开工前把这条再念一遍。**

> ### ✅ 2026-08-29 · 历史改写已执行
>
> `git filter-repo --replace-text --mailmap`，84 个提交全部重写，两个分支都强推过。
>
> | 做了什么 | 结果 |
> |---|---|
> | 替换真实内网网关地址 → `192.0.2.1` | 全历史零命中 |
> | 替换内网主机名、两处机器身份串 | 全历史零命中 |
> | 五个作者身份归一到一个 | `git log --all --format='%ae' \| sort -u` 只剩一个 |
> | 提交数 | 84 → 84，**一个没丢**；提交消息完好 |
>
> ⚠️ **一条差点犯的错**：`scripts/check.sh` 的脱敏黑名单里**本身就含那个主机名**
> （70 处）。如果按裸词做全局替换，**会把检查器自己弄瞎**——
> 替换规则只能用完整串（`<主机名>-mac-mini`、`<用户>@<主机>.local`），不能用裸词。
> **以后再改写历史，先 `git grep` 看看命中都在哪些文件里再写规则。**
>
> 📌 **备份**：改写前打了 `git bundle`（全 ref）+ `.git` 目录副本，
> 存在 `~/anyserver-backup-<时间戳>/`，已 `git bundle verify` 校验。
> **确认无误之前不要删。**
>
> ⚠️ **其它机器上的 clone 全部失效**——历史对不上，`git pull` 会失败。
> **必须重新 clone，不能 pull。**

- [ ] 通读 `git log`，确认历史提交里没有凭证
      （**历史删不干净**——真有的话要重写历史或换个新仓库）
- [ ] 确认 `.gitignore` 生效——**按文件名而不是路径匹配**，否则
      `ops/secrets.md` 会误报：
      ```sh
      git ls-files | xargs -n1 basename | grep -xE '\.env.*|.*\.(key|pem)|credentials\.json'
      ```
      应为空
