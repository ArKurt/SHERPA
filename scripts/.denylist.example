# 仓库作者的私有词表 —— 复制成 scripts/.denylist（已 gitignore），一行一个词。
#
# 放什么：你自己的主机名、内网标识、订阅/机场名、任何不该出现在公开仓库里的专名。
# 为什么不直接写进 check.sh：那等于用检查器把它们公开了。
# 这条是公开前审查查出来的——当时检查器源码里存着 8 个内部主机名，
# 而它还排除自己，所以永远不会因此变红。
#
# 示例（换成你自己的）：
# my-nas
# my-laptop
# some-internal-project-name
